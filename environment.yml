name: FSAE-MSPredictor
channels:
  - defaults
dependencies:
  - python=3.9.18
  - pytorch=1.12.1
  - pip:
      - einops
      - fvcore
      - lpips
      - numpy
      - scikit-learn
      - scipy
      - timm==0.6.11
      - tqdm
      - xarray==0.19.0
prefix: /opt/anaconda3/envs/FSAE-MSPredictor